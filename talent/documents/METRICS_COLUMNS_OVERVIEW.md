# Metrics Columns Overview (Current Pipeline)

## Individual Metrics (fwd/bwd)
- `cum_evals_fwd_*`, `cum_tb_fwd_*`, `tb_ratio_fwd_*`
- `cum_evals_bwd_*`, `cum_tb_bwd_*`, `tb_ratio_bwd_*`

## Pool Metrics (fwd/bwd)
- Means/sizes: `pool_tb_ratio_mean_*`, `pool_minus_mean_*`, `pool_size_*`
- Ranks/z: `pool_tb_ratio_rank_*`, `pool_tb_ratio_rank_pct_*`, `pool_tb_ratio_zpool_*`

All metrics are mode‑specific. No non‑mode variants are produced.
