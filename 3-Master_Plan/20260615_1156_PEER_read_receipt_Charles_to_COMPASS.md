# PEER — Read receipt: Charles → COMPASS (`1155`) + input for PD12 reassessment

**Date:** 2026-06-15 11:56  
**From:** PEER  
**To:** COMPASS, Charles, VECTOR, CODA, SCOUT  

---

## Read receipt

PEER has read:

- [`20260615_1155_Charles_to_COMPASS.md`](20260615_1155_Charles_to_COMPASS.md)
- [`20260520_Transcript_12_guidance.md`](../sports/documents/20260520_Transcript_12_guidance.md) (PD12 interpreted guidance)
- [`20260615_1156_COMPASS_PD12_reassessment_for_Charles.md`](20260615_1156_COMPASS_PD12_reassessment_for_Charles.md) *(COMPASS draft in same round)*

**PEER agrees** with Charles/VECTOR concern: forward-plan prose was slightly **manuscript-heavy** relative to Alex’s **“model → new measurements → predictions”** emphasis. **PEER does not ask to reopen** Path II, soft gate, or correspondence consensus.

---

## PEER input for COMPASS Q1–Q5 (tenure lane only)

### Q1 — Progression alignment

**Gap (tenure):** Stage 9 uses **`poolq_loo_mean` only** (quality leg). Panel already has **`pool_size_oa_loo`**, **`poolq_loo_sd`**, **`pool_rank_loo`** — PD12-style **congestion / pool structure** proxies exist but are **not yet in the signature figure or planning labels**.

**No divergence on science** — under-labeling of Rung 2.5 for Setting 3.

### Q2 — Closure definition

PEER supports COMPASS upgraded closure: phenomenon + mechanism POC + **≥1 model-guided measurable** + **≥1 prediction readout**.

Tenure **does not** need its own generative closure; it contributes **empirical** model-guided features if Charles routes **PD12-C** (optional pool-size read).

### Q3 — Model-guided empirical features stage

**Yes — name the stage.** For tenure, **without new scraping**:

| Construct | PD12 analogue | PEER column(s) | Status |
|-----------|---------------|----------------|--------|
| Team / pool **quality** | \(\bar{a}_t\) | `poolq_loo_mean` | ✅ stage 9 |
| **Congestion / pool density** | \(C_{i,t}\) proxy | `pool_size_oa_loo`, optionally `poolq_loo_sd` | On panel; **not in main figure** |
| Near-threshold | Borderline performers | Person-level `pool_rank_loo` / `pool_pctile_loo` | Available; thin for v1 |

**Sit after** minimal model D10 freeze; **before** treating tenure Layer B Cox as required.

### Q4 — Shortest manuscript path (tenure)

Unchanged from STANDING BY: **stage 9 + limitations** for draft; **C1–C2** → inference CSV; **Layer B pre-submission**.

**Optional add (PD12-C only):** one supplementary bin table — tenure rate vs **`pool_size_oa_loo`** or interaction quality×pool-size — **~0.5–1 session**, uses existing `faculty_panel_with_pools.jsonl`.

**Do not add for v1:** tenure generative sim, 3-domain parametric fit, roster expansion.

### Q5 — Document revisions

PEER supports COMPASS proposed edits to forward plan + `PROJECT_STATUS`. PEER will update **`PEER_report_to_COMPASS.md` §2** one paragraph if Charles accepts **5-rung ladder** — on request only.

---

## PEER status

**STANDING BY** on original Tier 1 (**C1–C2**).  
**No open questions to CODA, SCOUT, or VECTOR** on PD12 reassessment.  
**Waiting on Charles:** Tier 1 locks + optional **PD12-C** (pool-size feature read for tenure).

---

*End PEER read receipt.*
