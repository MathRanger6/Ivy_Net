# Disposable perf-metric thread — YOU ARE HERE

**One-sentence COMPASS line:** H_sort is assign (ρ) context only; LOO-shape batch confirms **keep PPM** for reigning hero — alternates fail convex β₂ test.

---

## YOU ARE HERE

| Step | Task | Status |
|------|------|--------|
| 1 | H_sort ladder | **Done** |
| 2 | Acronym-first glossary | **Done** |
| 3 | COMPASS kill criterion documented | **Done** |
| 4 | LOO-shape batch (draft rate vs poolq_LOO + LPM β₂) | **Done** — `loo_shape/LOO_SHAPE_REPORT.md` |
| 5 | Optional ρ calibration per metric | **Parked** — not needed unless LOO pass found |

---

## LOO-shape results (reigning porch · 2009–2021)

| Verdict | Metrics | Note |
|---------|---------|------|
| **marginal** | `ppm` | β₂ ≈ +0.0017 — reigning lock baseline |
| **fail** (convex β₂) | `fg_pct`, `efg_pct`, `ts_pct_box`, `tspct`, `per`, `bpm`, `ws` | More convex than PPM; BPM worst (+0.069) |
| **pass** (concave β₂) | `minutes` only | Concave but opportunity axis, not ability |

**Campaign:** No metric fork. ρ ≈ 0 on PPM stays locked; proceed γ, λ, t + Pass B.

---

## Thread log

- **2026-08-28** — LOO batch wired (`perf_metric_loo_shape_batch.py`); all ladder metrics run; no promotion candidate for hero switch.
