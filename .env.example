# Copy to `.env` in this folder (repo root). `.env` is git-ignored.
# For notebooks: export these in the shell before `jupyter`, or use a tool that loads `.env`.

# OpenAlex (optional Bearer — improves rate limits vs mailto-only)
OPENALEX_API_KEY=

# Optional override for OpenAlex polite-pool identity (mailto= on API URLs)
# OPENALEX_MAILTO=hgt6rn@virginia.edu
